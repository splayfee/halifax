# ultimate-express and hyper-express depend on uWebSockets.js, which is published only as
# a git repository (not on the npm registry). pnpm blocks such "exotic" transitive deps by
# default; allow them so these optional HTTP-adapter peers install cleanly in CI and locally.
block-exotic-subdeps=false
